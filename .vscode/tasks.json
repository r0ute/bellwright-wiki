{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Clean generated docs",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "Get-ChildItem -Path .\\docs -File -Filter *.md | Remove-Item -Force; if (Test-Path .\\docs\\assets) { Get-ChildItem -Path .\\docs\\assets -Exclude *.js,*.css | Remove-Item -Recurse -Force }"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "Generate docs",
      "type": "shell",
      "command": "python",
      "args": [
        "-m",
        "generator"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "dependsOn": [
        "Clean generated docs"
      ],
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
